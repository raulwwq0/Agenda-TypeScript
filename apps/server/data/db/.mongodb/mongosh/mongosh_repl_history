use agenda
db.updateUser("root", { roles: [{ role: "readWrite", db: "agenda" }] })
db.updateUser("root",{  roles: [})
show collections
db.createCollection("contacts")
db.createCollection(contacts)
use agenda
exit
use agenda
exit
use agenda user: "root", pwd: "toor", roles: [] })
use agenda user: "root", pwd: "toor", roles: [] })
use agenda